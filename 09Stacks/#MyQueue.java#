import java.util.*;

public class MyQueue<T>{

    private LinkedList<T> queue;
    
    
    public T enqueue(T item){
	return item;
    }

    public T dequeue(){
	LinkedList<T> now = queue;
	if (queue.getData() != null){
	    now = queue.getNext();
	    
	}
    }
}
